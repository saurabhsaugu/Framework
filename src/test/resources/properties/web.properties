# Web test configuration (loaded by PropertyConfig)
# Controls where web tests run: local | container | saucelabs
web.environment=local

# Browser settings
web.browserName=edge
web.headless=false

# Remote Selenium/Grid URL (used when web.environment=container)
web.remote.url=http://docker-host:4444/wd/hub